Install all of the needed dependencies in the requirements.txt file with 'pip install -r requirements.txt' in your shell. To run the app locally enter the following command: 'streamlit run "./main.py"'

